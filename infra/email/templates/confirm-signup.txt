Vantage — confirm your email address

Hi,

Someone created a Vantage account with {{ .Email }}.
Confirm this address to finish signup and open your research workspace.

Confirm your email:
{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=signup

One-time code (if the link expires early) — enter on the sign-in page under “Have a confirmation code?”:
{{ .Token }}

This link and code expire in 24 hours and can be used only once.

If you did not create this account, ignore this email. No account is activated until the address is confirmed.

Useful links
------------
Sign in: {{ .SiteURL }}/login
Research Library: {{ .SiteURL }}/library
Support: {{ .SiteURL }}/support
Start research: {{ .SiteURL }}/research/new

---
Why you received this
Required account security message for signup at {{ .Email }}.
You cannot unsubscribe from security emails.

Email preferences: {{ .SiteURL }}/settings/notifications
Unsubscribe from product updates: {{ .SiteURL }}/unsubscribe?email={{ .Email }}

Vantage — market pain research
{{ .SiteURL }}
Vantage Research

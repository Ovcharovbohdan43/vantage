Vantage — reset your password

Hi,

We received a request to reset the password for {{ .Email }}.
Use the link below to choose a new password and get back into your workspace.

Reset your password:
{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=recovery

One-time code (if the link expires early) — enter on the password reset page with your email:
{{ .Token }}

This link and code expire in 1 hour and can be used only once.

Didn’t request this? Ignore this email. Your password will not change unless someone completes the reset flow.

Always reset passwords from official Vantage emails or:
{{ .SiteURL }}/forgot-password

Useful links
------------
Sign in: {{ .SiteURL }}/login
Forgot password: {{ .SiteURL }}/forgot-password
Support: {{ .SiteURL }}/support
Account: {{ .SiteURL }}/account

---
Why you received this
Required account security message because a password reset was requested for {{ .Email }}.
You cannot unsubscribe from security emails.

Email preferences: {{ .SiteURL }}/settings/notifications
Unsubscribe from product updates: {{ .SiteURL }}/unsubscribe?email={{ .Email }}

Vantage — market pain research
{{ .SiteURL }}
Vantage Research
